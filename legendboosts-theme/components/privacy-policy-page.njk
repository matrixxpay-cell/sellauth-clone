<section id="{{ componentId }}" class="component lb-policy-page" data-component-id="{{ componentId }}">
  <h1 class="lb-policy-title">{{ properties.title or 'Privacy Policy' }}</h1>
  <div class="lb-prose lb-policy-content">{{ shop.privacy_policy | safe }}</div>
</section>
